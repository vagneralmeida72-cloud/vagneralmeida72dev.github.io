unit SistemaClientes;

interface

uses
  Winapi.Windows,
  Winapi.Messages,
  System.SysUtils,
  System.Variants,
  System.Classes,
  Vcl.Graphics,
  Vcl.Controls,
  Vcl.Forms,
  Vcl.Dialogs,
  Vcl.Imaging.pngimage,
  Vcl.ExtCtrls,
  Vcl.Buttons,
  Vcl.Menus;

type
  TfrmSistemaClientes = class(TForm)
    MainMenu1: TMainMenu;
    teste1: TMenuItem;
    Cadastro1: TMenuItem;
    Sair1: TMenuItem;

    procedure Sair1Click(Sender: TObject);
    procedure FormCreate(Sender: TObject);

  private

  public

  end;

var
  frmSistemaClientes: TfrmSistemaClientes;

implementation

{$R *.dfm}

procedure TfrmSistemaClientes.FormCreate(Sender: TObject);
begin
 Self.Menu := MainMenu1;
end;

procedure TfrmSistemaClientes.Sair1Click(Sender: TObject);
begin
  Close;
end;

end.
