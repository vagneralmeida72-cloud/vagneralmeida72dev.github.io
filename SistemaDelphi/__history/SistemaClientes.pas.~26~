unit SistemaClientes;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes,
  Vcl.Graphics, Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.Menus, Vcl.Skia, Vcl.ExtCtrls;

type
  TfrmPrincipal = class(TForm)
    MainMenu1: TMainMenu;
    S1: TMenuItem;
    Cadastros1: TMenuItem;
    Processos1: TMenuItem;
    Relatrios1: TMenuItem;
    Configuraes1: TMenuItem;
    Configuraes2: TMenuItem;
    N1: TMenuItem;
    Usurios1: TMenuItem;
    Clientes1: TMenuItem;
    Produtos1: TMenuItem;
    MovimentaodeProdutos1: TMenuItem;
    Clientes2: TMenuItem;
    Produtos2: TMenuItem;
    Produtos3: TMenuItem;
    Image1: TImage;
    estaApi1: TMenuItem;
    procedure Configuraes2Click(Sender: TObject);
    procedure Clientes1Click(Sender: TObject);
    procedure Clientes2Click(Sender: TObject);
    procedure estaApi1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmPrincipal: TfrmPrincipal;

implementation

//uses uCadastroCliente, uRelCliente, uApi;

{$R *.dfm}

procedure TfrmPrincipal.Clientes1Click(Sender: TObject);
begin
//  frmCadastroCliente := TfrmCadastroCliente.Create(Self);
//  try
//    frmCadastroCliente.ShowModal;
//  finally
//    frmCadastroCliente.Free;
//  end;
end;

procedure TfrmPrincipal.Clientes2Click(Sender: TObject);
begin
//  frmRelCliente := TfrmRelCliente.Create(Self);
//  try
//    frmRelCliente.P_GerarRelatorio;
//  finally
//    frmRelCliente.Free;
//    frmRelCliente := nil;
//  end;
end;

procedure TfrmPrincipal.Configuraes2Click(Sender: TObject);
begin
  Close;
end;

procedure TfrmPrincipal.estaApi1Click(Sender: TObject);
begin
//  frmApi := TfrmApi.Create(Self);
//  try
//    frmApi.ShowModal;
//  finally
//    frmApi.Free;
//  end;
end;

end.


end.
